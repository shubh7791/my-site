All collection items must start at the same column (3:1)
   1 | # This is a basic workflow to help you get started with Actions
   2 |
>  3 | name: Build & Deploy
     | ^^^^^^^^^^^^^^^^^^^^
>  4 |
     | ^
>  5 | # Controls when the workflow will run on:
     | ^
>  6 |   # Triggers the workflow on push or pull request events but only for the main branch
     | ^
>  7 |   push:
     | ^
>  8 |     branches: [ main ]
     | ^
>  9 |
     | ^
> 10 | # A workflow run is made up of one or more jobs that can run sequentially or in parallel
     | ^
> 11 | jobs:
     | ^
> 12 |   # This workflow contains a single job called "build"
     | ^
> 13 |   deploy:
     | ^
> 14 |     # The type of runner that the job will run on
     | ^
> 15 |     runs-on: ubuntu-latest
     | ^
> 16 |
     | ^
> 17 |     # Steps represent a sequence of tasks that will be executed as part of the job
     | ^
> 18 |     steps:
     | ^
> 19 |       # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
     | ^
> 20 |       - name: Deploy Bootstrap 5 static webpage for my-site
     | ^
> 21 |         uses: appleboy/ssh-action@master # call a custom script by user appleboy for SSH on github
     | ^
> 22 |         with: # all below inputs are specifically defined in ssh-action script
     | ^
> 23 |           host: ${{secrets.SSH_HOST}} # IP address of the server
     | ^
> 24 |           key: ${{secrets.SSH_KEY}} #private key of the server
     | ^
> 25 |           username: ${{secrets.SSH_USERNAME}} # user of the server
     | ^
> 26 |           
     | ^
> 27 |           script: |
     | ^
> 28 |             cd /var/www/my-site # enter into the folder that will host the landing page for the first time
     | ^
> 29 |             git pull # update the working directory with the latest from the github repo
     | ^
> 30 |             echo 'Deployment successful to Digital Ocean' # Display message to reflect deployment status
     | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
